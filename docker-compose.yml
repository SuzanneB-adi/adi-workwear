version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: adi_workwear
    ports:
      - 5432:5432
    volumes:
      - db_data:/var/lib/postgresql/data

  app:
    build: .
    environment:
      DATABASE_URL: "postgresql://postgres:postgres@db:5432/adi_workwear?schema=public"
    ports:
      - 3000:3000
    depends_on:
      - db

volumes:
  db_data:

